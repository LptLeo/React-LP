/**
 * Página de gestão de dados de contato (a ser implementada).
 */
import { Card } from "@/shared/components/ui/Card";

export function ContactPage() {
  return (
    <Card>
      <h1 className="font-display text-text text-2xl font-semibold">Contato</h1>
      <p className="text-secondary mt-2">
        Gestão de contato em desenvolvimento (próxima etapa).
      </p>
    </Card>
  );
}
